Matplotlib YAML Plot Specification (MYPlotSpec) is a Python package used to
write Matplotlib-based plotting scripts that may be configured using YAML.

The intended purpose is to help users rapidly write scripts to plot new types of
data, while retaining access to the detailed control of plot settings using
matplotlib.

Requirements:
    - matplotlib
    - yaml
    - numpy
    - six
